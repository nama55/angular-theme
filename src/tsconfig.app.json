{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": ["node", "jquery", "bootstrap", "jqueryui"]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
